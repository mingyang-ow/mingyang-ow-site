# mingyang-ow-site

Personal portfolio site — plain HTML/CSS with a little vanilla JS. No frameworks, no build step.

## Structure

```
public/   ← all site files served from here
```

## Deploy

Push to `main`. GitHub Actions publishes to GitHub Pages automatically.
